import AppKit

/// Custom views for the menu-bar dropdown (stubbed — Claude status removed).
enum AttentionMenu {
  static let rowWidth: CGFloat = 256

  /// Render stub (no-op — Claude status removed).
  static func debugRender(to path: String) {}
}
